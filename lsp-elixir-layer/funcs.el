;; (defun lsp-elixir/config-lsp-client ()

;;   ;; Set up the lsp-mode client for use with Elixir
;;   (lsp-define-stdio-client
;;    lsp-elixir
;;    "elixir"
;;    (lambda () default-directory)
;;    elixir-ls-executable
;;    )
;; )
